import { migrateContent } from "../src/utils/content-migration"; import path from "path"; const config = { sourceDir: path.resolve(__dirname, "../../docs"), targetDir: path.resolve(__dirname, "../src/content"), languages: ["zh", "en", "ko", "ja"] }; console.log("开始迁移内容..."); console.log("源目录：", config.sourceDir); console.log("目标目录：", config.targetDir); migrateContent(config).then(() => { console.log("内容迁移完成！"); }).catch((error) => { console.error("迁移过程中出现错误：", error); process.exit(1); });
